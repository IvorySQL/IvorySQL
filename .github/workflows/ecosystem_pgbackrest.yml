name: Ecosystem pgBackRest integration

on:
  push:
    branches: [agent/pgbackrest-recovery]
    paths:
      - tools/ecosystem/pgbackrest/**
      - .github/workflows/ecosystem_pgbackrest.yml
  pull_request:
    paths:
      - tools/ecosystem/pgbackrest/**
      - .github/workflows/ecosystem_pgbackrest.yml
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    defaults:
      run:
        working-directory: tools/ecosystem/pgbackrest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - name: Run harness unit tests
        run: python -m unittest discover -s tests -v
      - name: Validate shell syntax
        run: bash -n scripts/*.sh
      - name: Validate Compose model
        run: |
          cp .env.example .env
          docker compose config --quiet
      - name: Check patch whitespace
        run: git show --check --oneline HEAD -- .github/workflows/ecosystem_pgbackrest.yml tools/ecosystem/pgbackrest

  integration:
    if: github.event_name == 'push'
    needs: validate
    runs-on: ubuntu-latest
    timeout-minutes: 60
    defaults:
      run:
        working-directory: tools/ecosystem/pgbackrest
    steps:
      - uses: actions/checkout@v6
      - name: Configure test secrets
        run: cp .env.example .env
      - name: Build and start database
        run: make build up
      - name: Verify backup and repository policy
        run: make verify
      - name: Verify isolated restore
        run: make restore-verify
      - name: Collect service logs
        if: failure()
        run: docker compose logs --no-color --tail 200
      - name: Remove test cluster
        if: always()
        run: make clean
